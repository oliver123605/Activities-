//variables
let subject = "mobile programming";
let passingGrade = 75;
let teacher = "mr. yuri ortiz";
let totalStudents=0;

//arrays

let names = ["oliver", "raven", "theo"];
let scores = [85,70,95];
let results =[];

//loop

for(let i=0;1< scores.length;i++){
    if(scores[i]>= passinggrade){
        results.push("passar");
    }else{
       results.push("bagsak"); 

    }
    }

    for (let i=0;i<names.length;i++){
        totalStudents= totalStudents + 1;

    }

    let i=0;
    while (i< name.length){
        if (scores[i]>=90) {
            console.log(names[i]+ "scored" + scores[i] + "-" + results[i]);
        }
         if (scores[i]>=90){
             console.log(names[i]+ "scored" + scores[i] + "-" + results[i]);
         }
         if (scores[i]>=90){
            console.log(names[i]+ "scored" + scores[i] + "-" + results[i]);
         }
         i = i +1;
    

    }
console.log("Subject: " + subject);
console.log("Teacher: " + teacher);
console.log("Totalstudents: " + totalstudents);
